{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "runtime": "v2",
    "preDeployCommand": "python manage.py collectstatic --noinput",
    "startCommand": "gunicorn app.wsgi --log-file -"
  }
}
